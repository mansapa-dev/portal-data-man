import{TeachersService}from'./teachers';
describe('TeachersService list contract',()=>{it('supports account status and returns homeroom classes without internal identifiers',async()=>{const findMany=jest.fn().mockResolvedValue([{publicId:'TEACHER_PUBLIC_ID_0000001',fullName:'Guru',status:'ACTIVE',account:{status:'ACTIVE'},classes:[{publicId:'CLASS_PUBLIC_ID_00000001',code:'XII.1'}]}]),count=jest.fn().mockResolvedValue(1),db={teacher:{findMany,count},$transaction:jest.fn(async(values:Promise<unknown>[])=>Promise.all(values))};const service=new TeachersService(db as never,{}as never);const result=await service.list({page:1,perPage:25,sortDirection:'asc',accountStatus:'ACTIVE'}as never);expect(findMany).toHaveBeenCalledWith(expect.objectContaining({where:expect.objectContaining({account:{status:'ACTIVE'}}),include:expect.objectContaining({classes:expect.any(Object)})}));expect(result.data[0]).toMatchObject({publicId:'TEACHER_PUBLIC_ID_0000001',account:{status:'ACTIVE'},classes:[{code:'XII.1'}]});expect(result.data[0]).not.toHaveProperty('id')})});
