import{PrismaClient}from'@prisma/client';import*as argon2 from'argon2';import{ulid}from'ulid';const db=new PrismaClient();async function main(){const{name,email,password}={name:process.env.SUPER_ADMIN_NAME,email:process.env.SUPER_ADMIN_EMAIL,password:process.env.SUPER_ADMIN_PASSWORD};if(!name||!email||!password||password.length<12)throw new Error('SUPER_ADMIN_NAME, SUPER_ADMIN_EMAIL dan SUPER_ADMIN_PASSWORD (minimal 12 karakter) wajib diisi.');await db.adminUser.upsert({where:{email:email.toLowerCase()},update:{name},create:{publicId:ulid(),name,email:email.toLowerCase(),passwordHash:await argon2.hash(password,{type:argon2.argon2id}),role:'SUPER_ADMIN',status:'ACTIVE'}});}main().finally(()=>db.$disconnect());
