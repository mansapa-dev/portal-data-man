import{CanActivate,ExecutionContext,Injectable,UnauthorizedException}from'@nestjs/common';import{SessionService}from'../session.service';
@Injectable()export class TeacherAuthGuard implements CanActivate{constructor(private sessions:SessionService){}async canActivate(c:ExecutionContext){const req=c.switchToHttp().getRequest(),session=await this.sessions.resolveTeacher(req.cookies?.portal_teacher_session),account=session?.teacherAccount,teacher=account?.teacher;if(!account||!teacher||teacher.deletedAt||teacher.status!=='ACTIVE'||account.status!=='ACTIVE'||account.lockedUntil&&account.lockedUntil>new Date())throw new UnauthorizedException('Sesi guru tidak valid.');req.teacher=teacher;req.teacherAccount=account;req.teacherSession=session;return true;}}
