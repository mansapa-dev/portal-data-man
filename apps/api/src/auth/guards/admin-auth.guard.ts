import { CanActivate,ExecutionContext,Injectable,UnauthorizedException } from '@nestjs/common'; import { SessionService } from '../session.service';
@Injectable() export class AdminAuthGuard implements CanActivate {constructor(private sessions:SessionService){} async canActivate(c:ExecutionContext){const req=c.switchToHttp().getRequest();const session=await this.sessions.resolve(req.cookies?.portal_session);if(!session?.adminUser||session.adminUser.deletedAt||session.adminUser.status!=='ACTIVE')throw new UnauthorizedException('Sesi tidak valid.');req.user=session.adminUser;req.session=session;return true;} }
