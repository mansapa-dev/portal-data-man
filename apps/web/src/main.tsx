import React from'react';import{createRoot}from'react-dom/client';import{QueryClient,QueryClientProvider,useQuery}from'@tanstack/react-query';import{BrowserRouter,NavLink,Route,Routes}from'react-router-dom';import{BookOpen,FileUp,GraduationCap,LayoutDashboard,School,Settings,ShieldCheck,Users}from'lucide-react';import'./styles.css';
const api=async(path:string)=>{const r=await fetch(`/api/v1${path}`,{credentials:'include'});if(!r.ok)throw new Error('Gagal mengambil data');return r.json()};
const menus=[['/','Dashboard',LayoutDashboard],['/students','Data Siswa',GraduationCap],['/teachers','Data Guru',Users],['/classes','Data Kelas',School],['/periods','Periode Akademik',BookOpen],['/imports','Import Data',FileUp],['/applications','Aplikasi',Settings],['/audit','Audit Log',ShieldCheck]]as const;
function Layout(){return <div className="shell"><aside><div className="brand"><span>PD</span><div>Portal Data<small>Sumber data sekolah</small></div></div><nav>{menus.map(([to,label,Icon])=><NavLink key={to} to={to} end={to==='/' }><Icon size={19}/>{label}</NavLink>)}</nav><div className="account">Administrator<small>SUPER ADMIN</small></div></aside><main><header><div><b>Portal Data</b><span>Pusat manajemen data sekolah</span></div><button>Profil</button></header><Routes><Route path="/" element={<Dashboard/>}/><Route path="/students" element={<Students/>}/><Route path="*" element={<Placeholder/>}/></Routes></main></div>}
function Dashboard(){return <Page title="Dashboard" subtitle="Ringkasan data sekolah hari ini"><div className="cards">{[['Siswa Aktif','690'],['Guru Aktif','—'],['Kelas','20'],['Akun Guru','—']].map(([a,b])=><article><small>{a}</small><strong>{b}</strong><span>Data terbaru</span></article>)}</div><section><h2>Selamat datang di Portal Data</h2><p>Kelola siswa, guru, kelas, periode akademik, dan integrasi aplikasi dari satu tempat.</p></section></Page>}
function Students(){const q=useQuery({queryKey:['students'],queryFn:()=>api('/students')});return <Page title="Data Siswa" subtitle="Kelola master data dan keanggotaan kelas"><div className="toolbar"><input aria-label="Cari siswa" placeholder="Cari nama atau NISN…"/><button className="primary">Tambah siswa</button></div><section className="tablewrap">{q.isPending?<p>Memuat data…</p>:q.isError?<p>Data gagal dimuat. Silakan coba lagi.</p>:<table><thead><tr><th>NISN</th><th>Nama siswa</th><th>Status</th><th>Diperbarui</th></tr></thead><tbody>{q.data.data.map((s:any)=><tr key={s.publicId}><td className="mono">{s.nisn}</td><td>{s.fullName}</td><td><span className="badge">{s.status}</span></td><td>{new Date(s.updatedAt).toLocaleDateString('id-ID')}</td></tr>)}</tbody></table>}</section></Page>}
function Placeholder(){return <Page title="Modul sedang disiapkan" subtitle="Fitur manajemen ini tersedia melalui arsitektur Portal Data"><section><p>Gunakan menu di samping untuk berpindah modul.</p></section></Page>};function Page(p:React.PropsWithChildren<{title:string;subtitle:string}>){return <div className="page"><div className="title"><h1>{p.title}</h1><p>{p.subtitle}</p></div>{p.children}</div>};const client=new QueryClient();createRoot(document.getElementById('root')!).render(<React.StrictMode><QueryClientProvider client={client}><BrowserRouter><Layout/></BrowserRouter></QueryClientProvider></React.StrictMode>);
