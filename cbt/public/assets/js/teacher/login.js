(async function(){'use strict';const form=document.getElementById('teacherLogin');const message=document.getElementById('message');let csrf='';try{const me=await fetch('api/auth/me',{credentials:'same-origin'}).then(r=>r.json());csrf=me.data.csrf_token;if(me.data.staff?.role==='TEACHER'){location.href='guru/dashboard';return;}}catch{message.textContent='Server tidak dapat dihubungi.';}form.addEventListener('submit',async e=>{e.preventDefault();message.textContent='Memverifikasi NIP...';const button=form.querySelector('button');button.disabled=true;try{const response=await fetch('api/auth/teacher/login',{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json','X-CSRF-Token':csrf},body:JSON.stringify({nip:document.getElementById('nip').value,password:document.getElementById('password').value})});const result=await response.json();if(!response.ok)throw new Error(result.message||'Login gagal.');location.href='guru/dashboard';}catch(error){message.textContent=error.message;}finally{button.disabled=false;}});})();
