(async function(){'use strict';const content=document.getElementById('content');const notice=document.getElementById('notice');let csrf='';let data={ujianList:[],hasilList:[],pelanggaranList:[]};const el=(tag,text,className)=>{const node=document.createElement(tag);if(text!==undefined)node.textContent=String(text);if(className)node.className=className;return node;};const panel=(title,description)=>{const box=el('article',undefined,'panel');box.append(el('h3',title),el('p',description));return box;};function table(headers,rows){const table=el('table');const head=el('thead'),tr=el('tr');headers.forEach(h=>tr.append(el('th',h)));head.append(tr);const body=el('tbody');if(!rows.length){const empty=el('tr'),cell=el('td','Belum ada data.');cell.colSpan=headers.length;empty.append(cell);body.append(empty);}else rows.forEach(row=>{const line=el('tr');row.forEach(value=>line.append(el('td',value??'-')));body.append(line);});table.append(head,body);return table;}function render(section){content.replaceChildren();document.querySelectorAll('.nav-item').forEach(b=>b.classList.toggle('active',b.dataset.section===section));if(section==='exams'){const box=panel('Ujian & Mapel yang Diampu','Hanya ujian yang ditugaskan admin kepada NIP Anda.');box.append(table(['Nama Ujian','Tingkat','Sesi','Tahun Ajaran'],data.ujianList.map(x=>[x.nama_ujian,x.tingkat,x.sesi,x.tahun_ajaran])));content.append(box);}if(section==='results'){const box=panel('Hasil Siswa','Nilai peserta pada ujian yang Anda ampu.');box.append(table(['NISN','Nama','Kelas','Ujian','Nilai','Status'],data.hasilList.map(x=>[x.nomor_ujian,x.nama_siswa,x.kelas,x.nama_ujian,x.nilai,x.status])));content.append(box);}if(section==='violations'){const box=panel('Log Pelanggaran','Pelanggaran peserta pada ujian yang Anda ampu.');box.append(table(['Waktu','NISN','Nama','Ujian','Jumlah','Keterangan'],data.pelanggaranList.map(x=>[x.waktu,x.nomor_ujian,x.nama_siswa,x.nama_ujian,x.jumlah_pelanggaran,x.keterangan])));content.append(box);}if(section==='account'){const box=panel('Ubah Password','Password baru minimal 12 karakter.');const form=el('form');form.style.cssText='display:grid;gap:14px;max-width:460px';[['Password lama','old'],['Password baru','new']].forEach(([label,id])=>{const wrap=el('label',label);wrap.style.cssText='display:grid;gap:7px;font-weight:700;font-size:13px';const input=el('input');input.type='password';input.id=id;input.required=true;input.style.cssText='padding:12px;border:1px solid var(--line);border-radius:10px';wrap.append(input);form.append(wrap);});const button=el('button','Simpan Password');button.style.cssText='border:0;border-radius:10px;padding:12px;background:var(--green);color:#fff;font-weight:800';form.append(button);form.addEventListener('submit',changePassword);box.append(form);content.append(box);}}async function changePassword(e){e.preventDefault();try{await api('api/auth/password','POST',{old_password:document.getElementById('old').value,new_password:document.getElementById('new').value});notice.style.color='var(--green-dark)';notice.textContent='Password berhasil diperbarui.';e.target.reset();}catch(error){notice.style.color='#c0392b';notice.textContent=error.message;}}async function api(path,method='GET',body){const response=await fetch('../'+path,{method,credentials:'same-origin',headers:{'Content-Type':'application/json','X-CSRF-Token':csrf},body:body===undefined?undefined:JSON.stringify(body)});const result=await response.json();if(!response.ok)throw new Error(result.message||'Permintaan gagal.');return result;}try{const me=await fetch('../api/auth/me',{credentials:'same-origin'}).then(r=>r.json());if(me.data.staff?.role!=='TEACHER'){location.href='../guru';return;}csrf=me.data.csrf_token;document.getElementById('teacherName').textContent=me.data.staff.nip||'Guru';data=(await api('api/teacher/dashboard')).data;render('exams');}catch(error){notice.textContent=error.message;}document.querySelectorAll('.nav-item').forEach(button=>button.addEventListener('click',()=>render(button.dataset.section)));document.getElementById('menu').addEventListener('click',()=>document.querySelector('.sidebar').classList.toggle('open'));document.getElementById('logout').addEventListener('click',async()=>{try{await api('api/auth/logout','POST',{});}finally{location.href='../guru';}});})();
