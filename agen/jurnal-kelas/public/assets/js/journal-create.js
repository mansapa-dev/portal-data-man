const form=document.querySelector('#journal-create'),select=form.elements.attendancePublicId,error=document.querySelector('#journal-error'),csrf=document.querySelector('meta[name="csrf-token"]').content;try{const response=await fetch('/api/journals/available-attendance',{credentials:'same-origin'}),body=await response.json();if(!response.ok)throw new Error(body.message);select.innerHTML='<option value="">Pilih absensi final</option>'+body.data.map(a=>`<option value="${a.publicId}">${a.date} · ${a.className} · ${a.subjectName} · Jam ${a.periodStart}${a.periodEnd!==a.periodStart?'–'+a.periodEnd:''}</option>`).join('');if(!body.data.length)error.textContent='Belum ada absensi final yang dapat dibuatkan jurnal.',error.hidden=false}catch(e){error.textContent=e.message;error.hidden=false}form.addEventListener('submit',async event=>{event.preventDefault();const button=form.querySelector('button');button.disabled=true;error.hidden=true;try{const response=await fetch('/api/journals',{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json','X-CSRF-Token':csrf},body:JSON.stringify(Object.fromEntries(new FormData(form)))}),body=await response.json();if(!response.ok)throw new Error(body.message);location.assign(`/journals/${body.data.journal.public_id}`)}catch(e){error.textContent=e.message;error.hidden=false;button.disabled=false}});
