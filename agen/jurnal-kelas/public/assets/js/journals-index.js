const list=document.querySelector('#journal-list'),search=document.querySelector('#journal-search'),status=document.querySelector('#journal-status');let timer;
async function load(){const query=new URLSearchParams(location.search);if(search.value)query.set('search',search.value);else query.delete('search');if(status.value)query.set('status',status.value);else query.delete('status');history.replaceState(null,'',`${location.pathname}?${query}`);const response=await fetch(`/api/journals?${query}`,{credentials:'same-origin'}),body=await response.json();if(!response.ok){list.innerHTML=`<p>${body.message}</p>`;return}list.innerHTML=body.data.items.length?body.data.items.map(j=>`<a href="/journals/${j.publicId}"><div><strong>${escapeHtml(j.className)} · ${escapeHtml(j.subjectName)}</strong><span>${j.journalDate} · Jam ${j.periodStart}${j.periodEnd!==j.periodStart?'–'+j.periodEnd:''}</span><p>${escapeHtml(j.topic.length>120?j.topic.slice(0,120)+'…':j.topic)}</p></div><b>${j.status}</b></a>`).join(''):'<p>Belum ada jurnal sesuai filter.</p>'}function changed(){clearTimeout(timer);timer=setTimeout(load,250)}search.value=new URLSearchParams(location.search).get('search')??'';status.value=new URLSearchParams(location.search).get('status')??'';search.addEventListener('input',changed);status.addEventListener('change',load);function escapeHtml(v){const d=document.createElement('div');d.textContent=v;return d.innerHTML}load();
